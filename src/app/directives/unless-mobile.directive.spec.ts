import { UnlessMobileDirective } from './unless-mobile.directive';

describe('UnlessMobileDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlessMobileDirective();
    expect(directive).toBeTruthy();
  });
});
