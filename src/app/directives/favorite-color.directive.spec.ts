import { FavoriteColorDirective } from './favorite-color.directive';

describe('FavoriteColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FavoriteColorDirective();
    expect(directive).toBeTruthy();
  });
});
